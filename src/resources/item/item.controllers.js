import { crudControllers } from '../../utils/crud'
import { Item } from './item.model'

export default crudControllers(Item)

// you can overite these controllers see the transcript fro wiring up crud controllers
